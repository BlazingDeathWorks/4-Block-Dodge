using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockCollisionHandlerManager : MonoBehaviour
{
    /*private void Awake()
    {
        BlockCollisionHandler[] blockCollisionHandlers = (BlockCollisionHandler[])FindObjectsOfType(typeof(BlockCollisionHandler));
        if (blockCollisionHandlers == null) return;
        foreach(BlockCollisionHandler blockCollisionHandler in blockCollisionHandlers)
        {
            BlockCollision.BlockCollisionEvent += blockCollisionHandler.OnBlockCollision;
        }
    }*/
}
